fn main() {
    /*
    let x = true;
    
    match x {
        true => println!("truee"),
        false => println!("faaaalse"),
    }
    // */
    /*
    let x = 5;
    let rem = x%5;

    let phrase = match rem {
        0 => "REmainder is zero",
        1 => "REmainder is one",
        2 => "REmainder is two",
        3 => "REmainder is three",
        _ => "#",//println!(),
    };
    println!("{}",phrase);
    // */
    /* 
    let num = 84;
    let divisible_by_two = if num % 2 == 0 {
        "even"
    }
    else{"odd"};
    println!("{}",divisible_by_two);
    // */

    //* 
    let nums = [1, 2, 3, 4, 5];

    // for each loop
    for num in nums.iter() {
        println!("{} ", num);
    }
    // */
    
}
